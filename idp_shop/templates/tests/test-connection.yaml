apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "idp_shop.fullname" . }}-test-connection"
  labels:
    {{- include "idp_shop.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "idp_shop.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
