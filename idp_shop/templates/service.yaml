apiVersion: v1
kind: Service
metadata:
  name: {{ include "idp_shop.fullname" . }}
  labels:
    {{- include "idp_shop.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "idp_shop.selectorLabels" . | nindent 4 }}
